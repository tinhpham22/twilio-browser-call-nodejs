extends ../layout

block content
  // Main jumbotron
  .jumbotron.bicycle-polo-background
    .container.bicycle-polo-text
      h1.text-center
        strong Birchwood Bicycle Polo Co.
      p
        | We sell only the finest bicycle polo supplies for those seeking fame and glory in this 
        a(href="https://www.youtube.com/watch?v=DaK9Zj3QHDY", target="_blank") sport of kings
        | .
      h3
        strong Having trouble with one of our products?
      p
        | Talk to one of our support agents now &#8212; or fill out the support form below and someone will call you later.
      p
        a.btn.btn-primary.btn-lg.btn-block(href="#support", role="button") Get help
        
  .container
    if(success.length > 0)
      .alert.alert-success !{success}
    if (errors.length > 0)
      .alert.alert-danger !{errors}
    h2#support User information
    .row
      .col-md-6.col-md-push-6
      
      .col-md-6.col-md-pull-6
        form(class="new-user")
          .form-group
            label UserName
            input.form-control(type='text', name="userName", placeholder='Andry Zhukov')
          .form-group
            label Phone Number:
            input.form-control(type='text', name="phoneNumber", placeholder='+16887665773')
          .form-group
            label Identity:
            input.form-control(type='text', name="identity", placeholder='abc')
          .form-group
            label AppSid:
            input.form-control(type='text', name="appSid", placeholder='abc')
          .form-group
            label ApiKey:
            input.form-control(type='text', name="apiKey", placeholder='abc')
          .form-group
            label ApiSecret:
            input.form-control(type='text', name="apiSecret", placeholder='abc')
          .form-group
            label AccountSid:
            input.form-control(type='text', name="accountSid", placeholder='abc')
          .form-group
            label Twilio Phone:
            input.form-control(type='text', name="twilioPhone", placeholder='+1xxxxxxx')
          button.btn.btn-primary.btn-lg.btn-block(type='button') Submit
            
      small.pull-right
        | Header photo courtesy
        a(href="https://www.flickr.com/photos/tink20seven/4654348885/") David Sachs via Flickr
